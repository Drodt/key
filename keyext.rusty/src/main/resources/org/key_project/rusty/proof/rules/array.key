\sorts {
    Field;
    Array<[E, const N: int]>;
}

\functions {
    arr(int) -> Field;
    len<[E, const N: int]>(Array<[E, const N]>) -> int;
    arr_get<[E, const N: int]>(Array<[E, const N]>, Field) -> E;
    arr_set<[E, const N: int]>(Array<[E, const N]>, Field, E) -> Array<[E, const N]>;
}
