\sorts {
    Array<[E, const N: int]>;
}

\functions {
    len<[E, const N: int]>(Array<[E, const N]>) -> int;
    arr_repeat<[E, const N: int]>(E, int) -> Array<[E, const N]>;
    arr_outside<[E]> -> E;
    arr_get<[E, const N: int]>(Array<[E, const N]>, int) -> E;
    arr_set<[E, const N: int]>(Array<[E, const N]>, int, E) -> Array<[E, const N]>;
}
